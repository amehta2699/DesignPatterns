package src.DecoratorPattern.Decorator;

import src.DecoratorPattern.BasePizza;

// Topping decorator is-a BasePizza and has-a BasePizza -> Relationship

// This pattern is used to add extra features on top of base features
public abstract class ToppingDecorator extends BasePizza {
}
